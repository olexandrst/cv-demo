# Works on Python 3.10–3.14 (no TensorFlow / DeepFace).
# All face / emotion / gender / age inference goes through cv2.dnn, so we
# don't need onnxruntime as a separate dependency.

flask>=3.0,<4.0
opencv-python>=4.8,<5.0
numpy>=1.26,<3.0
pillow>=10.0
ultralytics>=8.3,<9.0
huggingface_hub>=0.23
